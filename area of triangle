

base = float(input('Please Enter the Base of a Triangle: '))

height = float(input('Please Enter the Height of a Triangle: '))

area = (base * height / 2 ) 
print("The Area of a Triangle using =", area)
